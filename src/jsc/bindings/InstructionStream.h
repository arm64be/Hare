#pragma once

// BytecodeGeneratorBase.h is a pinned private SDK compatibility copy. Its
// quoted include resolves from this Bun include directory, so forward it to
// the packaged JavaScriptCore SDK without modifying the pinned copy.
#include <JavaScriptCore/InstructionStream.h>
